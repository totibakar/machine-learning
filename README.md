Machine Learning stuff <br> <br>

Installing the lib:<br>
1. python -m pip install pandas
2. python -m pip install numpy scikit-learn matplotlib
3. kalau error, copy psate ".\.venv\Scripts\Activate.ps1" di terminal (titik\titikvenv)
4. terus coba lagi "pip install pandas numpy scikit-learn matplotlib" 
5. python -m pip install Sastrawi
6. python -m pip install matplotlib
7. python -m pip install seaborn
